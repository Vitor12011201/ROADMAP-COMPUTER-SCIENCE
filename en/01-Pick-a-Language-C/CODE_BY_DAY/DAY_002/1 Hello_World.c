#include <stdio.h>  // Header Files (.h): Files like stdio.h provide basic tools
#include <stdlib.h> // Preprocessor (#): Acts before the compiler

int main(void) { // The program always starts at the main() function
    printf("GitHub\n"); // \n: Command to skip to a new line (newline).
    system("pause");
    return 0;
} // The program always ends at the closing brace '}'